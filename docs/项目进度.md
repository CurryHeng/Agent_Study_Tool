# StudyForge 项目进度

> 滚动更新的进度快照。最近更新：2026-08-14（审计问题修复 + 简答题 LLM 判分 + 导入 Agent 迁移）。

## 一、当前基线

- **后端**：`168` 个测试全部通过，`ruff check` 全绿，`/api/health` 正常。
- **前端**：`10` 个单元测试通过，`vue-tsc` / `vite build` 通过。
- **数据库**：12 表，迁移 `55497f64d4b9` 与模型一致。

## 二、P0 功能范围（需求 §6，全部完成）

| P0 项 | 状态 | 说明 |
|---|---|---|
| ① 资料导入 | ✅ | 上传/列表/删除/重建向量索引（≤10MB） |
| ② 文档解析 | ✅ | PDF/Word/PPT/Markdown/TXT/HTML；图片为后置 |
| ③ 知识点提取 | ✅ | 规则引擎 + LLM 抽样交叉验证（导入 Agent，见下） |
| ④ 思维导图 | ✅ | markmap 渲染 |
| ⑤ AI 自动出题 | ✅ | 生成 + 三层校验（需 `DEEPSEEK_API_KEY`） |
| ⑥ AI 自动审题 | ✅ | Review Agent + FAIL 回环（≤2 次） |
| ⑦ 题库管理 | ✅ | CRUD / 软删除 / 收藏 / 举一反三 / 搜索 |
| ⑧ 刷题 | ✅ | 宽松/普通/严格三模式 + 判题 + SM-2 |
| ⑨ 错题记录 | ✅ | 答错生成 / 编辑错因 / 筛选 / 收藏 |
| ⑩ Navigator Agent | ✅（主体） | Navigator/Orchestrator/Question/Review 独立节点 |
| 简答题判题 | ✅ | LLM 判分（`grade_short_answer`），LLM 不可用降级为用户自评 |

## 三、本轮已完成（2026-08-14）

### 3.1 审计问题修复（依据《问题小结》#1~#10、#15）

- **#1 [P0]** stats 复习卡查询补 `user_id` 过滤（跨用户统计泄露）
- **#2 [P1]** 删除被学习练习册不再 500（显式清理 answer/wrong 记录后级联）
- **#3 [P1]** Markdown 渲染 XSS：公式抽离后整体 `escapeHtml`，零依赖消毒
- **#4** 上传大小前后端统一为 10MB
- **#5** Agent graph `workbook_id` 缺失友好 400（不再 KeyError→500）
- **#6** 未配置 `DEEPSEEK_API_KEY` 返回 503 友好文案（`LLMNotConfiguredError`）
- **#7** Agent 任务日志按 errors/异常记 `failed`（不再恒 success）
- **#8** 错题列表批量取题/知识点，消除 N+1
- **#9** `/api/questions` 回填 `knowledge_name`（list/get/create/update）
- **#10** ruff E501 ×2；**#15** 删除死代码 `MindMapBranch.vue` 及其测试
- **简答题 LLM 判分**：`grading.grade_short_answer`（空白判错不调 LLM；LLM 异常/未配 key 降级 `is_correct=None` 转用户自评）

### 3.2 导入 Agent 迁移（自 agent-quiz-feature，TS→Python 重写）

将另一开发者在旧 React/Express 栈实现的导入工作流移植到现架构（`backend/`）：

- **`services/structure_extract.py`**：中英双语规则引擎（语言检测 / 标题 / 粗体·「」术语 / 定义·对比·列表正则 / 段落级分块 / 术语匹配统计），0 次 LLM
- **`services/knowledge_extract_service.py`**：规则预提取、均匀抽样、短路判断、知识点三层校验、验证缓存（`parsed/{doc_id}.validated.json`）、LLM 块级提取（失败/解析异常均降级为空数组）
- **`workflow/import_graph.py`**：LangGraph 导入工作流
  `START →（无有效章节时）document_agent（LLM 章节理解）→ knowledge_agent（规则→抽样交叉验证→LLM 决策→必要时全量提取）→ validate（三层校验，失败回环 ≤2 次）→ END`
- **接入上传链路**：`document_service.upload_document` 仅在配置 `DEEPSEEK_API_KEY` 时运行增强；Document Agent 可重建章节；知识点经 `knowledge_service.import_knowledge_points` 挂入知识树（按章节标题匹配，去重；importance/difficulty 不入库）
- **同步延迟保护**：全量提取 LLM 调用上限 `MAX_FULL_EXTRACT_CHUNKS = 30`
- 移植时修正原实现两处问题：编号列表条目误取数字组导致被丢弃；提取 LLM 异常未捕获会击穿工作流
- 测试：`tests/test_import_agent.py` 16 例（规则引擎/分块/抽样/校验/图流程/LLM 故障降级/上传集成）
- 未迁移：图片视觉 OCR（对方用 deepseek-chat 当视觉模型实际不可用；待换千问视觉后单独做）

### 3.3 内置题库替换 + 开发者账号 + 遗留清理（2026-08-14）

- **内置题库整体替换**：删除旧 154 道填空题（含关联答题/错题/复习卡/知识节点），新内置题库 = 《深入理解 AI Agent》第 1-2 章（`backend/seed/agent_bank.py`）：22 题（单选 8 / 多选 4 / 判断 4 / 填空 4 / 简答 2）+ 15 个知识节点（2 章 13 知识点）。审计 #17（seed 题型单一）随之解决
- **`seed/seed.py` 重写**：幂等——确保系统账号/工作簿、清空旧内置题并重灌；不再依赖旧 `server/src` 的 questions.json
- **开发者账号**：`dev` / `dev123456`（仅本地测试，登录即可用内置题库刷题）
- **删除旧 React/Express 遗留**：根目录 `server/ src/ packages/ dist/ test/ public/ scripts/ node_modules/` 及 `package.json vite/ts*config biome eslint tailwind 配置、index.html、start.bat` 等 22 项；项目现在只剩 `backend/ frontend/ docs/ 启动.bat AGENTS.md README.md`
- **README.md 重写**：旧 React 说明替换为当前架构 + 启动/账号/测试说明

## 四、未完成工作清单

### P0 残留（需求列为第一阶段，未做完整）

| 项 | 缺口 |
|---|---|
| Navigator Agent | 任务规划为单层意图→固定 plan；导入链路未接入 Navigator 统一入口（导入图为独立工作流） |
| 图片导入 | `parsers/image.py` 抛 501，需千问视觉（需求列为"后续扩展"） |

### P1（需求 §7，全部未开始）

- AI 错因分析（现仅有正则关键词分类雏形）
- 薄弱知识点强化（雏形：错题本 + 知识点热力图）
- AI 导师
- 知识图谱
- 知识点掌握度（雏形：`GET /api/stats`）
- 学习热力图（雏形：同上）

### P2（全部未开始）

- 每日学习计划、考前冲刺、个人笔记

### 工程/质量类

| 来源 | 事项 |
|---|---|
| F12 | 刷题 session 恢复（localStorage） |
| F9 | 前端 Playwright E2E（现为冒烟脚本） |
| B5 | 事务边界显式化（UoW），删文档可能留向量孤儿 |
| 后置 | api_keys 表接入、Tool 层、权限矩阵、LangGraph checkpoint |
| #11 | 列表无分页（wrong-records/questions/knowledge 全量返回） |
| #12 | WrongRecordUpdate 无法把字段清空（None=不改） |
| #13 | 软删题的错题仍显示在错题本 |
| #14 | 错题题型筛选在前端客户端做，未下沉后端 |
| #16 | auth store `loggedIn` 不随 token 过期/外部 clearAuth 同步 |
| ~~#17~~ | ✅ 已解决：内置题库替换为 Agent 第 1-2 章题库（5 种题型） |
| 其他 | 填空题精确匹配（多答案/近义不判对）；`/api/exams/{id}/result` 未实现（已判定可接受） |

## 五、下一步建议（按优先级）

1. **P1 主线**：AI 错因分析 → 薄弱知识点强化 → 知识点掌握度/热力图增强 → AI 导师
2. 图片解析（千问视觉，`qwen_api_key` 已预留）
3. 导入 Agent 接入 Navigator 统一入口（generate_mindmap/list_documents 之外加 import 意图）
4. P3 小项（#11~#17）与工程项（session 恢复、E2E、UoW）穿插进行
