# EStudy 开发任务分工（P0-P2 冲刺）

> 2026-08-15 重构：任务按**编码顺序与重要性**排列（依赖先行），负责人为列。
> 架构依据：《（必读）Agent架构分析》《（必读）总体设计》《（必读）详细设计》。
> 成员：A（张，联调）、B（蔡，LLM/题库）、C（梓恒，导图）、D（王悦，Agent）。
> Day 1 全员：`backend/.env` 填 `DEEPSEEK_API_KEY` → 启动前后端 → 走通核心链路 → 读 AGENTS.md §5/§6 与本文 §五契约。

---

## 一、P0：演示阻塞项

| # | 任务 | 编码顺序理由 | 负责人 | 验收样例 |
|---|---|---|---|---|
| P0-1 | **AgentChatView**（聊天页骨架：消息流+steps 展示+通用确认卡片+navigate 跳转） | 新架构唯一用户可见入口，后续所有 Agent 能力的容器 | A | 侧边栏"AI 助手"可对话（先用现有 /agent/chat） |
| P0-2 | **AI 出题 UI**（题库页生成入口+审题结果展示） | 核心卖点演示；依赖仅现有 /questions/generate | B | 选知识点+题型+数量→生成→显示"通过 N 道/驳回原因" |
| P0-3 | **思维导图显示修复** | 演示直接可见的 bug，工作量最小 | C | 节点名不再出现 `tmpXXX.tmp` 等异常 |
| P0-4 | **文件导入真实验证**（PDF/Word/PPT 各一份实测：解析→知识树→导图→RAG 全通） | 答辩现场要真实上传教材，PDF 解析从未用真实文件验证过 | C | 三种格式上传后解析→知识树→导图→RAG 全链路无报错 |
| P0-5 | **主 Agent ReAct 化 + 工具层**（graph.py 重构 create_react_agent + tools.py，从 P1 升入） | 新架构核心，演示的Agent 能力载体 | D | "整理资料并出 10 道题"自主完成，steps 可见 |

## 二、P1：本轮冲刺主任务（按依赖顺序编码）

| # | 任务 | 编码顺序理由 | 负责人 | 验收样例 |
|---|---|---|---|---|
| P1-1 | **接口契约冻结**：proposal 结构、chat 响应格式、错因 schema、知识编辑接口签名 | 所有人的依赖源头，Day1 上午必须定死 | 全员（D 主持） | 契约写入本文 §五，不再变更 |
| P1-2 | **知识节点编辑接口**（PUT/POST/DELETE /knowledge/{id}） | D 的工具层和 C 的前端都依赖它 | B | 接口可用，不破坏 import_knowledge_points 写入 |
| P1-3 | **错因分析服务**（coach_service v1 + WrongRecord 错因字段） | A 的热力图聚合、D 的教练工具依赖其 schema | C | 错题点"分析错因"→结构化归因 `{reason_type, explanation, suggestion}` |
| P1-5 | **写操作两阶段确认**（/agent/confirm + 写工具 proposal 化） | 依赖 P0-5；A 的确认卡片依赖其结构 | D | propose 不落库、confirm 才落库（有测试） |
| P1-6 | **上下文注入 + steps 上报 + 多轮** | 依赖 P0-5 | D | "把这里改简单点"定位选中节点；"再来 5 道难的"理解上文 |
| P1-7 | **真实 LLM 出题/审题调优** | 独立任务，P1-4 完成后联调 | C | 真实生成 20 题，驳回率 <30% |
| P1-8 | **填空题 LLM 判分** | 独立小任务 | C | "反向传播算法"/"BP算法"近义判对 |
| P1-9 | **学习热力图**（掌握度着色 + 日历活跃图） | 依赖 P1-3 错因 schema | A | ①知识点掌握度着色可点击钻取错题 ②日历式学习活跃热力图 |
| P1-10 | **P0 项接入新 Agent**（P0-1/2 的 UI 对接 D 的新接口） | 最后联调，依赖 P0-5/P1-5/P1-6 | A | 聊天全流程演示可用 |
| P1-11 | **对话历史后端**（conversations/messages 两表 + CRUD API + /agent/chat 接入持久化与上下文加载） | 依赖 P1-1 契约；是 P1-6 多轮的数据地基 | D | 聊天内容落库，传 conversation_id 可续聊 |
| P1-12 | **聊天页会话列表**（新建/切换/删除会话） | 依赖 P1-11 接口 | A | 左侧会话列表可管理，刷新后历史仍在 |

## 三、P2：下一轮增强（本轮有余力才动）

| # | 任务 | 负责人 | 说明 |
|---|---|---|---|
| P2-1 | 图片导入（千问视觉） | A | parsers/image.py 接 QWEN_API_KEY |
| P2-2 | create_plan 学习计划工具 | D+C | 导师模式核心能力 |
| P2-3 | 导图 LLM 分支生成 | B | 选节点"AI 扩展"建议子分支 |
| P2-4 | LangGraph checkpoint 持久化 | D | 重启不丢任务状态 |
| P2-5 | FSRS 参数个性化 | C | 按用户答题历史优化权重 |

## 四、P3：质量债（穿插进行，不阻塞）

列表分页、错题筛选下沉后端、刷题 session 恢复、Playwright E2E、UoW 事务边界、知识图谱、学习活动时间线。

---

## 五、共享接口契约（Day1 定死，变更须在群里声明）

### 协作任务标注（🔗 = 需联合完成，issue 已打"协作"标签）

| 协作对 | 双方 | 对齐内容 |
|---|---|---|
| #36 契约冻结 | 全员 | 四项契约，Day1 上午 |
| #33 聊天页 × #39 ReAct 化 | A×D | chat 接口契约，UI 骨架先行 |
| #39 × #61 知识编辑接口 | D×C | 接口签名，D 包装为工具 |
| #40 两阶段确认 × #45 联调 | D×A | proposal 结构 → 确认卡片 |
| #41 上下文注入 × #45 | D×A | context 字段结构 |
| #46 对话历史后端 × #47 会话列表 | D×A | conversations API 响应结构 |
| #38 错因分析 × #44 热力图 | B×A | reason_type schema |
| #34 出题 UI × #42 generate 响应扩展 | B 内部 | generate 响应含审题结果 |

### 契约表

| 契约                       | 涉及    | 要点                                                                                                    |
| ------------------------ | ----- | ----------------------------------------------------------------------------------------------------- |
| 写操作 proposal 结构          | D↔A   | `{proposal_id, action, target, changes, impact}`；`POST /api/agent/confirm {proposal_id, approved}` 执行 |
| `/api/agent/chat` 响应     | D↔A   | 含 `reply` + `steps[]` + `proposals[]`；请求含 `context{view, selected_knowledge_id, ...}`                 |
| 错因字段 schema              | B↔A   | `{reason_type, explanation, suggestion}`，热力图按 reason_type 聚合                                          |
| 知识树编辑接口                  | C↔B   | C 的 PUT/DELETE 不得破坏 `import_knowledge_points`；删除沿用 SET NULL                                           |
| `/questions/generate` 响应 | B↔A   | 含每题审题结果（前端展示驳回原因）                                                                                     |
| LLMService               | B/C/D | 只加方法不改已有签名；prompt 常量放各自模块                                                                             |
| .env                     | A     | DEEPSEEK_API_KEY（文本）、QWEN_API_KEY（视觉），互不侵占                                                            |

## 六、Day 5 验收演示脚本

登录 → 上传 PDF →（自动索引+导图生成）→ 聊天让 AI 助手出 10 题（steps 可见）→ 确认提案入库 → 题库看审题结果 → 刷题故意答错（FSRS）→ 错题 AI 错因分析 → 热力图看薄弱点 → 聊天"把第三章改简单点"→确认提案 → 全程 pytest/vitest 全绿。

## 七、分工速查

| 成员    | GitHub       | 任务                                  |
| ----- | ------------ | ----------------------------------- |
| A（张）  | Raccoon0725  | P0-1、P1-9、P1-10、P1-12、P2-1 |
| B      | CurryHeng    | P0-2、P1-3、P1-7、P1-8、P2-5（智能题库域）|
| C      | caixuejun222 | P0-3、P0-4、P1-2、P2-3（知识可视化域）  |
| D（王悦）| Zephyr-ix    | P0-5、P1-1（主持）、P1-5、P1-6、P1-11、P2-2、P2-4 |

低耦合原则：改动集中在自己模块，跨模块只走 §五契约。
